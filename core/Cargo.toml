[package]
name = "asciidork-core"
version = "0.36.0"
edition = "2024"
description = "Asciidork core shared utils, types"
license = "MIT"

[dependencies]
lazy_static = "1.4.0"
regex = { version = "1.10.2", features = ["std", "use_std"] }

[lints]
workspace = true

[lib]
doctest = false
